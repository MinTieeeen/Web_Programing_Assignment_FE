[project]
name = "nextplay-selenium-tests"
version = "0.1.0"
description = "Selenium tests for NextPlay login and dropdown functionality"
requires-python = ">=3.9"
dependencies = [
    "selenium>=4.15.2",
    "webdriver-manager>=4.0.1",
]